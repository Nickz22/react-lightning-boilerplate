import React, { useState } from 'react';
import './ProgressBar.css';
const ProgressBar = ( {boxes} ) => {
    return (
        <div>
        {boxes}
        </div>
    );
}

export default ProgressBar;